name: 'Execute Powershell'

on:
  push:
    branches: [ action ]
    
jobs:

  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Powershell - prepare_pwsh.ps1
      run: .\prepare_pwsh.ps1